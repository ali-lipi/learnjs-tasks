—лучайное целое число от min до max
важность: 2
Ќапишите функцию randomInteger(min, max), котора€ генерирует случайное целое (integer) число от min до max (включительно).

Ћюбое число из интервала min..max должно по€вл€тьс€ с одинаковой веро€тностью.

ѕример работы функции:

alert( randomInteger(1, 5) ); // 1
alert( randomInteger(1, 5) ); // 3
alert( randomInteger(1, 5) ); // 5
ћожно использовать решение из предыдущей задачи.
-------------------------------------------------------------

(???)


=======================================================

ѕримечание (из учебника):
ѕростое, но неправильное решение
—амое простое, но неправильное решение Ц генерировать случайное число от min до max и округл€ть его:

function randomInteger(min, max) {
  let rand = min + Math.random() * (max - min);
  return Math.round(rand);
}

alert( randomInteger(1, 3) );
‘ункци€ будет работать, но неправильно. ¬еро€тность получить min и max значени€ в 2 раза меньше, чем любое другое число.

≈сли вы запустите приведЄнный выше пример, то заметите, что 2 по€вл€етс€ чаще всего.

Ёто происходит потому, что метод Math.round() получает случайные числа из интервала 1..3 и округл€ет их следующим образом:

число от 1    ... до 1.4999999999  округлитс€ до 1
число от 1.5  ... до 2.4999999999  округлитс€ до 2
число от 2.5  ... до 2.9999999999  округлитс€ до 3
“еперь становитс€ пон€тно, что 1 получает в 2 раза меньше значений, чем 2. “о же самое с 3.

ѕравильное решение задачи
≈сть много правильных решений этой задачи. ќдно из них Ц правильно указать границы интервала. „тобы выровн€ть интервалы, мы можем генерировать числа от 0.5 до 3.5, это позволит добавить необходимые веро€тности к min и max:

function randomInteger(min, max) {
  // получить случайное число от (min-0.5) до (max+0.5)
  let rand = min - 0.5 + Math.random() * (max - min + 1);
  return Math.round(rand);
}

alert( randomInteger(1, 3) );
ƒругое правильное решение Ц это использовать Math.floor дл€ получени€ случайного числа от min до max+1:

function randomInteger(min, max) {
  // случайное число от min до (max+1)
  let rand = min + Math.random() * (max + 1 - min);
  return Math.floor(rand);
}

alert( randomInteger(1, 3) );
“еперь все интервалы отображаютс€ следующим образом:

число от 1  ... до 1.9999999999  округлитс€ до 1
число от 2  ... до 2.9999999999  округлитс€ до 2
число от 3  ... до 3.9999999999  округлитс€ до 3
¬се интервалы имеют одинаковую длину, что выравнивает веро€тность получени€ случайных чисел.