Что выведет этот код?

важность: 5
Что выведет код ниже?

alert( null || 2 && 3 || 4 );


Решение:

3

Сначала выполняется 2 && 3 = 3 (выше приоритет, чем у ||).

null || 3 || 4


&& возвращает первое истинное значение => 3.